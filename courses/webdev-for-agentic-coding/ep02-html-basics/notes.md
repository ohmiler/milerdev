# EP02: HTML พื้นฐาน (HTML Basics)

## สิ่งที่จะได้เรียนรู้
- HTML คืออะไร ทำหน้าที่อะไร
- โครงสร้างของ HTML Tag
- Tags พื้นฐานที่ใช้บ่อย
- Semantic HTML
- Forms & Input Types

---

## 1. HTML คืออะไร

**HTML** = **H**yper**T**ext **M**arkup **L**anguage

- **ไม่ใช่** ภาษาโปรแกรม — เป็น **ภาษา Markup** (ใช้ "แท็ก" กำกับเนื้อหา)
- บอก Browser ว่าเนื้อหาแต่ละส่วน **คืออะไร** (หัวข้อ, ย่อหน้า, รูปภาพ, ลิงก์)
- เป็น **โครงกระดูก** ของทุกเว็บไซต์

---

## 2. โครงสร้างของ HTML Tag

```html
<p class="intro">สวัสดีชาวโลก</p>
│  │           │        │              │
│  │           │        │              └─ Closing Tag (แท็กปิด)
│  │           │        └─ Content (เนื้อหา)
│  │           └─ Attribute Value (ค่า)
│  └─ Attribute Name (ชื่อ attribute)
└─ Opening Tag (แท็กเปิด)
```

### ประเภทของ Tags
- **Opening + Closing Tag:** `<p>...</p>`, `<h1>...</h1>`, `<div>...</div>`
- **Self-closing Tag:** `<img>`, `<br>`, `<hr>`, `<input>` (ไม่ต้องมี closing tag)
- **Attributes:** ข้อมูลเพิ่มเติม เช่น `class="..."`, `id="..."`, `src="..."`, `href="..."`

---

## 3. โครงสร้าง HTML พื้นฐาน

```html
<!DOCTYPE html>           <!-- บอกว่าเป็น HTML5 -->
<html lang="th">          <!-- root element, ระบุภาษา -->
  <head>                   <!-- ข้อมูล metadata (ไม่แสดงบนหน้า) -->
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>เว็บไซต์แรกของฉัน</title>
  </head>
  <body>                   <!-- เนื้อหาที่แสดงผลจริง -->
    <h1>สวัสดี!</h1>
    <p>นี่คือเว็บไซต์แรกของฉัน</p>
  </body>
</html>
```

### อธิบายแต่ละส่วน
| ส่วน | หน้าที่ |
|------|---------|
| `<!DOCTYPE html>` | ประกาศว่าเป็น HTML5 |
| `<html>` | Root element ครอบทุกอย่าง |
| `<head>` | Metadata — title, charset, CSS links, meta tags |
| `<body>` | เนื้อหาที่แสดงให้ผู้ใช้เห็น |

---

## 4. Tags พื้นฐานที่ใช้บ่อย

### Headings (หัวข้อ)
```html
<h1>หัวข้อหลัก (ใหญ่สุด)</h1>
<h2>หัวข้อรอง</h2>
<h3>หัวข้อย่อย</h3>
<h4>หัวข้อย่อยย่อย</h4>
<h5>ระดับ 5</h5>
<h6>ระดับ 6 (เล็กสุด)</h6>
```

> 💡 ใช้ `<h1>` เพียง 1 ครั้งต่อหน้า และไม่ควรข้ามลำดับ (เช่น h1 → h3 โดยไม่มี h2)

### ข้อความ
```html
<p>ย่อหน้า — block element กินพื้นที่เต็มบรรทัด</p>
<strong>ตัวหนา — มีความหมายว่า "สำคัญ"</strong>
<em>ตัวเอียง — มีความหมายว่า "เน้น"</em>
<br>  <!-- ขึ้นบรรทัดใหม่ -->
<hr>  <!-- เส้นคั่นแนวนอน -->
<span>ข้อความ inline — ไม่กินพื้นที่เต็มบรรทัด</span>
```

### ลิงก์
```html
<a href="https://milerdev.com">เยี่ยมชม MilerDev</a>
<a href="https://milerdev.com" target="_blank">เปิดใน Tab ใหม่</a>
<a href="/about">ไปหน้า About (ลิงก์ภายใน)</a>
<a href="#section2">กระโดดไปส่วน section2</a>
```

### รูปภาพ
```html
<img src="photo.jpg" alt="คำอธิบายรูป" width="300">
<img src="https://example.com/image.png" alt="รูปจาก URL">
```

> ⚠️ `alt` attribute สำคัญมาก — ช่วยเรื่อง Accessibility (Screen readers) และ SEO

### รายการ (Lists)
```html
<!-- Unordered List (ไม่เรียงลำดับ — bullet points) -->
<ul>
  <li>HTML</li>
  <li>CSS</li>
  <li>JavaScript</li>
</ul>

<!-- Ordered List (เรียงลำดับ — ตัวเลข) -->
<ol>
  <li>เปิด Editor</li>
  <li>เขียนโค้ด</li>
  <li>เปิด Browser ดูผลลัพธ์</li>
</ol>
```

---

## 5. Block vs Inline Elements

| ประเภท | ลักษณะ | ตัวอย่าง |
|--------|--------|----------|
| **Block** | กินพื้นที่เต็มบรรทัด, ขึ้นบรรทัดใหม่เสมอ | `div`, `p`, `h1`-`h6`, `ul`, `ol`, `form` |
| **Inline** | อยู่ในบรรทัดเดียวกัน, กินพื้นที่เท่าเนื้อหา | `span`, `a`, `strong`, `em`, `img`, `code` |

```html
<!-- div = Block (กล่องใหญ่) -->
<div>
  <h2>หัวข้อ</h2>
  <p>เนื้อหา</p>
</div>

<!-- span = Inline (กล่องเล็ก ใน line เดียว) -->
<p>ราคา <span class="price">฿299</span> บาท</p>
```

---

## 6. Semantic HTML

Semantic HTML ใช้แท็กที่มี **ความหมาย** แทนการใช้ `div` ทุกอย่าง

### เปรียบเทียบ
```html
<!-- ❌ ไม่ Semantic -->
<div class="header">...</div>
<div class="nav">...</div>
<div class="main">...</div>
<div class="footer">...</div>

<!-- ✅ Semantic -->
<header>...</header>
<nav>...</nav>
<main>...</main>
<footer>...</footer>
```

### Semantic Tags ที่ใช้บ่อย
| Tag | ความหมาย |
|-----|----------|
| `<header>` | ส่วนหัวของหน้าหรือ section |
| `<nav>` | เมนูนำทาง |
| `<main>` | เนื้อหาหลัก (ใช้ 1 ครั้งต่อหน้า) |
| `<section>` | กลุ่มเนื้อหาที่เกี่ยวข้องกัน |
| `<article>` | เนื้อหาที่สมบูรณ์ในตัวเอง (บทความ, โพสต์) |
| `<aside>` | เนื้อหาเสริม (sidebar) |
| `<footer>` | ส่วนท้าย |
| `<figure>` + `<figcaption>` | รูปภาพพร้อมคำบรรยาย |

### ข้อดีของ Semantic HTML
1. **SEO ดีขึ้น** — Search Engine เข้าใจโครงสร้างเว็บ
2. **Accessibility** — Screen readers อ่านได้ถูกต้อง
3. **อ่านง่าย** — Developer อ่านโค้ดเข้าใจง่ายขึ้น
4. **AI เข้าใจ** — Agentic Coding tools วิเคราะห์โค้ดได้ดีขึ้น

---

## 7. Forms — ฟอร์มรับข้อมูล

### โครงสร้างฟอร์ม
```html
<form action="/submit" method="POST">
  <label for="name">ชื่อ:</label>
  <input type="text" id="name" name="name" required>

  <label for="email">อีเมล:</label>
  <input type="email" id="email" name="email">

  <label for="msg">ข้อความ:</label>
  <textarea id="msg" name="message"></textarea>

  <button type="submit">ส่ง</button>
</form>
```

### Input Types ที่ใช้บ่อย
| Type | ใช้สำหรับ | ตัวอย่าง |
|------|----------|----------|
| `text` | ข้อความทั่วไป | ชื่อ, ที่อยู่ |
| `email` | อีเมล (มี validation) | user@example.com |
| `password` | รหัสผ่าน (ซ่อนตัวอักษร) | •••••• |
| `number` | ตัวเลข | จำนวน, อายุ |
| `tel` | เบอร์โทรศัพท์ | 081-xxx-xxxx |
| `url` | URL | https://... |
| `date` | วันที่ | ปฏิทิน picker |
| `checkbox` | เลือกหลายตัวเลือก | ☑ ยอมรับเงื่อนไข |
| `radio` | เลือกตัวเลือกเดียว | ○ ชาย ● หญิง |
| `file` | อัปโหลดไฟล์ | รูปโปรไฟล์ |
| `submit` | ปุ่มส่งฟอร์ม | [ส่ง] |

### Attributes สำคัญ
- `required` — บังคับกรอก
- `placeholder="..."` — ข้อความตัวอย่างในช่อง
- `value="..."` — ค่าเริ่มต้น
- `disabled` — ปิดการใช้งาน
- `readonly` — อ่านอย่างเดียว

---

## แบบฝึกหัด

### ฝึกหัดที่ 1: สร้างหน้า Portfolio
สร้างไฟล์ `index.html` ที่มี:
- หัวข้อชื่อของคุณ (`h1`)
- ย่อหน้าแนะนำตัว (`p`)
- รายการทักษะ (`ul` + `li`)
- ลิงก์ไป social media (`a`)

### ฝึกหัดที่ 2: สร้างฟอร์มติดต่อ
สร้างฟอร์มที่มี:
- ช่องชื่อ (text)
- ช่องอีเมล (email)
- ช่องข้อความ (textarea)
- ปุ่มส่ง (submit)

### ฝึกหัดที่ 3: Semantic HTML
แปลงโค้ดนี้ให้เป็น Semantic HTML:
```html
<div class="header">
  <div class="nav">
    <a href="/">หน้าแรก</a>
    <a href="/about">เกี่ยวกับ</a>
  </div>
</div>
<div class="content">
  <div class="article">
    <div class="title">บทความ</div>
    <div class="text">เนื้อหา...</div>
  </div>
</div>
<div class="footer">© 2025</div>
```

---

## อ่านเพิ่มเติม
- [MDN: Getting started with HTML](https://developer.mozilla.org/en-US/docs/Learn/HTML/Introduction_to_HTML/Getting_started)
- [MDN: HTML elements reference](https://developer.mozilla.org/en-US/docs/Web/HTML/Element)
- [MDN: HTML forms](https://developer.mozilla.org/en-US/docs/Learn/Forms)
