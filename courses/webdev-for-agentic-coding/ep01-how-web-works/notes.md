# EP01: เว็บทำงานอย่างไร (How the Web Works)

## สิ่งที่จะได้เรียนรู้
- Internet กับ Web ต่างกันอย่างไร
- Client-Server Model คืออะไร
- HTTP/HTTPS ทำงานอย่างไร
- URL มีโครงสร้างแบบไหน
- Browser แสดงเว็บไซต์ได้อย่างไร

---

## 1. Internet vs Web

### Internet
- เครือข่ายคอมพิวเตอร์ขนาดใหญ่ทั่วโลกที่เชื่อมต่อกันผ่านสาย (สายทองแดง, ไฟเบอร์ออปติก, ดาวเทียม)
- เปรียบเหมือน **"ถนน"** ที่ข้อมูลวิ่งผ่าน
- ถูกสร้างขึ้นตั้งแต่ปี 1960s (ARPANET)

### Web (World Wide Web)
- **บริการหนึ่ง** ที่ทำงานอยู่บน Internet
- ถูกคิดค้นโดย Tim Berners-Lee ในปี 1989
- ประกอบด้วยเว็บไซต์ที่เชื่อมต่อกันด้วย **Hyperlinks**
- เราเข้าถึงผ่าน **Browser** (Chrome, Firefox, Safari)

> 💡 **จุดสำคัญ:** Web เป็นแค่ส่วนหนึ่งของ Internet — Email, Chat, Game Online ก็ใช้ Internet เหมือนกันแต่ไม่ใช่ Web

---

## 2. Client-Server Model

การทำงานของเว็บเป็นแบบ **Request-Response**:

```
Client (Browser)  →  Request  →  Server
Client (Browser)  ←  Response ←  Server
```

### Client (ฝั่งผู้ใช้)
- **Browser** ของคุณ เช่น Chrome, Firefox, Safari
- ทำหน้าที่ **ขอข้อมูล** (Request) จาก Server
- แสดงผลข้อมูลที่ได้รับให้เราเห็น

### Server (ฝั่งเซิร์ฟเวอร์)
- คอมพิวเตอร์ที่เก็บไฟล์เว็บไซต์ และพร้อมให้บริการตลอด 24 ชม.
- รับ Request แล้ว **ส่งข้อมูลกลับ** (Response)
- อาจมีการประมวลผล เช่น ดึงข้อมูลจาก Database ก่อนส่งกลับ

---

## 3. HTTP / HTTPS

### HTTP คืออะไร
- **H**yper**T**ext **T**ransfer **P**rotocol
- "ภาษา" หรือ "กฎ" ที่ Client กับ Server ใช้ในการสื่อสารกัน
- เป็นแบบ **Stateless** — แต่ละ Request เป็นอิสระจากกัน

### HTTPS คืออะไร
- HTTP + **S**ecure (SSL/TLS encryption)
- ข้อมูลถูก **เข้ารหัส** ระหว่างทาง — ป้องกันการดักจับ
- ปัจจุบันเป็น **มาตรฐาน** ของเว็บ (สังเกตรูปกุญแจ 🔒 ใน Address Bar)

### HTTP Methods ที่ใช้บ่อย

| Method | ทำอะไร | ตัวอย่าง |
|--------|--------|----------|
| **GET** | ขอข้อมูล (อ่าน) | เปิดหน้าเว็บ, ดูรูปภาพ |
| **POST** | ส่งข้อมูล (สร้างใหม่) | สมัครสมาชิก, โพสต์ข้อความ |
| **PUT** | อัปเดตข้อมูล (แก้ไข) | แก้ไขโปรไฟล์ |
| **DELETE** | ลบข้อมูล | ลบโพสต์, ลบบัญชี |

### HTTP Status Codes

| รหัส | ความหมาย | ตัวอย่าง |
|------|----------|----------|
| **2xx** | สำเร็จ ✅ | `200 OK`, `201 Created` |
| **3xx** | Redirect 🔀 | `301 Moved Permanently`, `302 Found` |
| **4xx** | Client Error ❌ | `404 Not Found`, `403 Forbidden`, `401 Unauthorized` |
| **5xx** | Server Error 💥 | `500 Internal Server Error`, `503 Service Unavailable` |

---

## 4. URL — ที่อยู่ของเว็บ

URL = **U**niform **R**esource **L**ocator — ที่อยู่ที่ระบุตำแหน่งของทรัพยากรบน Web

```
https://www.example.com:443/products/shoes?color=red&size=42#reviews
│       │               │   │              │                   │
│       │               │   │              │                   └─ Fragment (ส่วนในหน้า)
│       │               │   │              └─ Query Parameters (ข้อมูลเพิ่มเติม)
│       │               │   └─ Path (เส้นทาง)
│       │               └─ Port (ประตูเข้าออก)
│       └─ Domain / Host (ชื่อเว็บ)
└─ Protocol / Scheme (โปรโตคอล)
```

### แต่ละส่วนคืออะไร
- **Protocol:** `https://` — วิธีการสื่อสาร
- **Domain:** `www.example.com` — ชื่อเว็บไซต์ (ถูกแปลงเป็น IP โดย DNS)
- **Port:** `:443` — ประตูเข้าออก (ปกติซ่อนไว้ — HTTP=80, HTTPS=443)
- **Path:** `/products/shoes` — เส้นทางไปหน้าที่ต้องการ
- **Query:** `?color=red&size=42` — ข้อมูลเพิ่มเติมในรูปแบบ key=value
- **Fragment:** `#reviews` — กระโดดไปยังส่วนเฉพาะในหน้า

---

## 5. DNS — สมุดโทรศัพท์ของ Internet

### DNS คืออะไร
- **D**omain **N**ame **S**ystem
- แปลงชื่อเว็บที่คนจำง่าย → IP Address ที่คอมพิวเตอร์เข้าใจ

### ขั้นตอนการทำงาน
1. คุณพิมพ์ `google.com` ใน Browser
2. Browser ถาม DNS Server ว่า "google.com อยู่ IP อะไร?"
3. DNS ตอบกลับ `142.250.199.46`
4. Browser เชื่อมต่อไปยัง IP นั้น

> 💡 เหมือนการเปิดสมุดโทรศัพท์ หาเบอร์จากชื่อคน — DNS หา IP จากชื่อเว็บ

---

## 6. Browser Rendering — กระบวนการแสดงผลเว็บ

เมื่อ Browser ได้รับข้อมูลจาก Server จะผ่านขั้นตอนเหล่านี้:

1. **รับ HTML** — ไฟล์หลักที่บอกโครงสร้างของหน้า
2. **Parse HTML → DOM** — สร้างโครงสร้างแบบ Tree จาก HTML
3. **โหลด CSS → CSSOM** — สร้าง Style Rules
4. **รวม DOM + CSSOM → Render Tree** — รู้ว่าอะไรต้องแสดงและแสดงอย่างไร
5. **Layout** — คำนวณตำแหน่งและขนาดของแต่ละ element
6. **Paint** — วาดหน้าเว็บให้เราเห็นบนหน้าจอ
7. **โหลด JavaScript** — รันสคริปต์เพื่อเพิ่มความสามารถเชิงโต้ตอบ

---

## 7. สามเสาหลักของเว็บ: HTML + CSS + JavaScript

| | HTML | CSS | JavaScript |
|---|------|-----|------------|
| **บทบาท** | โครงสร้าง | รูปลักษณ์ | พฤติกรรม |
| **เปรียบเหมือน** | โครงกระดูก | เสื้อผ้า/การแต่งตัว | สมองและกล้ามเนื้อ |
| **ทำอะไร** | กำหนดว่ามีอะไรบ้าง | กำหนดว่าหน้าตาเป็นอย่างไร | ทำให้หน้าเว็บมีชีวิต |
| **ตัวอย่าง** | หัวข้อ, ย่อหน้า, รูปภาพ | สี, ขนาด, จัดวาง | คลิก, แอนิเมชัน, โหลดข้อมูล |

---

## 8. ทำไมต้องรู้สิ่งเหล่านี้ก่อนใช้ Agentic Coding

1. **อ่านโค้ดที่ AI สร้างให้ได้** — ต้องรู้ว่า HTML, CSS, JS ทำอะไร
2. **สั่ง AI ได้ตรงจุด** — รู้ศัพท์เทคนิค เช่น "เพิ่ม header", "ทำ responsive"
3. **Debug ได้** — เข้าใจ HTTP errors, เปิด DevTools ดู Network ได้
4. **ตัดสินใจได้** — เลือก framework, เข้าใจ trade-offs
5. **ไม่ถูกหลอก** — รู้ว่าโค้ดที่ AI เขียนมา ถูกหรือผิด

> ⚠️ AI เป็นเครื่องมือที่ทรงพลัง แต่ถ้าไม่มีพื้นฐาน — คุณจะไม่รู้ว่ามันเขียนผิดตอนไหน

---

## แบบฝึกหัด
1. เปิด Browser แล้วพิมพ์ URL ใดก็ได้ — ลองระบุส่วนต่างๆ ของ URL (Protocol, Domain, Path)
2. เปิด DevTools (กด F12) แล้วไปที่ Tab "Network" — ลองดูว่ามี Request/Response อะไรบ้าง
3. ลองเข้าเว็บที่ไม่มีอยู่จริง — สังเกต Status Code ที่ได้ (404)

---

## อ่านเพิ่มเติม
- [MDN: How the Web Works](https://developer.mozilla.org/en-US/docs/Learn/Getting_started_with_the_web/How_the_Web_works)
- [MDN: What is a URL?](https://developer.mozilla.org/en-US/docs/Learn/Common_questions/Web_mechanics/What_is_a_URL)
- [MDN: HTTP Overview](https://developer.mozilla.org/en-US/docs/Web/HTTP/Overview)
