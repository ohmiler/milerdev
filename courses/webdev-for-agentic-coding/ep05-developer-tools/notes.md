# EP05: เครื่องมือนักพัฒนา (Developer Tools)

## สิ่งที่จะได้เรียนรู้
- Chrome DevTools คืออะไร เปิดอย่างไร
- Elements Panel — ดู/แก้ HTML & CSS
- Console Panel — ดู log & รัน JavaScript
- Network Panel — ติดตาม HTTP Requests
- Application Panel — Storage & Cookies
- Device Mode — จำลองมือถือ
- ขั้นตอนการ Debug

---

## 1. Chrome DevTools คืออะไร

เครื่องมือที่ฝังอยู่ใน Chrome Browser สำหรับนักพัฒนา — ช่วยตรวจสอบ แก้ไข และ debug เว็บไซต์

### วิธีเปิด
| วิธี | Windows | Mac |
|------|---------|-----|
| Shortcut | `F12` หรือ `Ctrl+Shift+I` | `Cmd+Opt+I` |
| คลิกขวา | คลิกขวา → "Inspect" | คลิกขวา → "Inspect" |
| เมนู | Menu → More Tools → Developer Tools | Menu → More Tools → Developer Tools |

---

## 2. Elements Panel — ดู HTML & CSS

### สิ่งที่ทำได้
- **ดูโครงสร้าง DOM** — เห็น HTML tree ทั้งหมด
- **แก้ไข HTML สด** — ดับเบิลคลิก element เพื่อแก้ข้อความ
- **ดู CSS Styles** — แผงด้านขวาแสดง CSS rule ทั้งหมดที่ใช้กับ element
- **แก้ไข CSS สด** — คลิกค่า CSS เพื่อแก้ เห็นผลทันที
- **ดู Box Model** — Tab "Computed" แสดง margin, padding, border, content

### วิธีใช้
1. คลิกขวาที่ element → "Inspect" → กระโดดไปที่ element นั้น
2. ใช้ปุ่ม 🔍 (Select element) เพื่อชี้แล้วเลือก element
3. แก้ไข HTML: ดับเบิลคลิกที่ข้อความหรือ attribute
4. แก้ไข CSS: คลิกค่าใน Styles panel แล้วพิมพ์ค่าใหม่

### เคล็ดลับ
- กดปุ่ม `.cls` ใน Styles panel เพื่อเพิ่ม/ลบ class
- กดปุ่ม `+` เพื่อเพิ่ม CSS rule ใหม่
- Toggle checkbox ข้างหน้า property เพื่อเปิด/ปิดสไตล์ชั่วคราว
- ดู `:hover`, `:active`, `:focus` states ได้จากปุ่ม `:hov`

---

## 3. Console Panel — ดู Log & รัน JavaScript

### ดู Output
```javascript
console.log("ข้อความ");              // ข้อความปกติ
console.error("มีข้อผิดพลาด!");     // สีแดง
console.warn("คำเตือน");            // สีเหลือง
console.table(arrayOfObjects);       // แสดงเป็นตาราง
console.dir(element);                // แสดง properties ของ object
console.time("label");               // เริ่มจับเวลา
console.timeEnd("label");            // หยุดจับเวลา
```

### รัน JavaScript สด
- พิมพ์ JavaScript ใน Console แล้วกด Enter
- ทดสอบ `document.querySelector(".btn")` เพื่อหา element
- พิมพ์ชื่อตัวแปรเพื่อดูค่า
- ใช้ `$0` เพื่ออ้างอิง element ที่เลือกอยู่ใน Elements panel

### Console Utilities
```javascript
$0                    // element ที่เลือกใน Elements panel
$("selector")         // = document.querySelector()
$$("selector")        // = document.querySelectorAll()
copy(object)          // copy object ไปที่ clipboard
clear()               // ล้าง console
```

### อ่าน Error Messages
เมื่อเจอ error สีแดง ให้อ่าน:
1. **Error type** — TypeError, ReferenceError, SyntaxError
2. **Error message** — อธิบายว่าผิดอะไร
3. **File & Line number** — คลิกเพื่อกระโดดไปที่จุดที่ผิด

---

## 4. Network Panel — ติดตาม HTTP Requests

### สิ่งที่เห็น
- **ทุก request** ที่ Browser ส่ง (HTML, CSS, JS, images, fonts, API calls)
- **Status Code** — 200 (OK), 301 (Redirect), 404 (Not Found), 500 (Server Error)
- **Size** — ขนาดของ response
- **Time** — เวลาที่ใช้ในการ request

### Tab ย่อย (เมื่อคลิก request)
| Tab | แสดงอะไร |
|-----|----------|
| **Headers** | Request & Response headers, Method, URL |
| **Preview** | Response data แบบ formatted (JSON, HTML) |
| **Response** | Raw response data |
| **Timing** | Waterfall — เวลาแต่ละขั้นตอน |
| **Initiator** | อะไรเป็นตัวเรียก request นี้ |

### Filter ตามประเภท
- **All** — ทุก request
- **Fetch/XHR** — API calls (สำคัญสำหรับ debug!)
- **JS** — JavaScript files
- **CSS** — Stylesheets
- **Img** — Images
- **Doc** — HTML documents

### เคล็ดลับ
- ✅ ติ๊ก "Preserve log" เพื่อเก็บ log แม้เปลี่ยนหน้า
- ✅ ติ๊ก "Disable cache" เพื่อโหลดใหม่ทุกครั้ง (ตอนพัฒนา)
- คลิกขวาที่ request → "Copy as cURL" เพื่อ copy คำสั่ง

---

## 5. Application Panel — Storage & Cookies

### Local Storage
```javascript
// เก็บข้อมูล
localStorage.setItem("theme", "dark");

// อ่านข้อมูล
const theme = localStorage.getItem("theme"); // "dark"

// ลบ
localStorage.removeItem("theme");
localStorage.clear(); // ลบทั้งหมด
```
- ข้อมูลถาวร — ไม่หายแม้ปิด Browser
- เก็บได้เฉพาะ String — ถ้าเป็น Object ต้อง `JSON.stringify()`

### Session Storage
- เหมือน Local Storage แต่ **หายเมื่อปิด Tab**
- ใช้ `sessionStorage.setItem()` / `getItem()`

### Cookies
- ข้อมูลเล็กๆ ที่ Server ส่งมาเก็บใน Browser
- ใช้กับ authentication, tracking
- ดูและแก้ไขได้ใน Application panel

---

## 6. Device Mode — จำลองมือถือ

### วิธีเปิด
- คลิกปุ่ม 📱 (Toggle Device Toolbar) มุมบนซ้ายของ DevTools
- หรือกด `Ctrl+Shift+M` (Windows) / `Cmd+Shift+M` (Mac)

### สิ่งที่ทำได้
- เลือก device preset: iPhone, iPad, Samsung, Pixel
- ปรับขนาดหน้าจอแบบ custom
- จำลอง Touch Events
- ทดสอบ Responsive Design
- จำลอง network speed (3G, 4G)

---

## 7. ขั้นตอนการ Debug (Debugging Flow)

### เจอปัญหา — ทำตามนี้:

1. **เปิด Console** — มี error สีแดงไหม?
2. **อ่าน error message** — บอกว่าผิดอะไร อยู่ที่ไฟล์ไหน บรรทัดอะไร
3. **เพิ่ม console.log()** — ใส่ในจุดที่สงสัยเพื่อดูค่าตัวแปร
4. **ดู Network** — API call สำเร็จหรือ fail? Response ถูกต้องไหม?
5. **ตรวจ Elements** — HTML/CSS ถูกต้องไหม? class ถูกเพิ่ม/ลบหรือเปล่า?
6. **ส่งให้ AI** — Copy error message + screenshot → ให้ AI วิเคราะห์

### Error Types ที่พบบ่อย
| Error | ความหมาย | วิธีแก้ |
|-------|----------|---------|
| `ReferenceError: x is not defined` | ตัวแปร x ไม่มีอยู่ | ตรวจชื่อตัวแปร, ประกาศก่อนใช้ |
| `TypeError: Cannot read property of undefined` | พยายามอ่าน property จาก undefined | ตรวจว่า object มีค่าก่อนเข้าถึง |
| `SyntaxError` | เขียนโค้ดผิดไวยากรณ์ | ตรวจวงเล็บ, เครื่องหมาย, comma |
| `404 Not Found` | ไม่เจอไฟล์/API | ตรวจ URL, path ให้ถูกต้อง |
| `CORS error` | Cross-Origin ถูกบล็อก | ตั้งค่า CORS ที่ Server |

---

## 8. Shortcuts ที่ต้องรู้

| Shortcut (Windows) | ทำอะไร |
|--------------------|--------|
| `F12` | เปิด/ปิด DevTools |
| `Ctrl+Shift+C` | Inspect element mode |
| `Ctrl+Shift+J` | เปิด Console โดยตรง |
| `Ctrl+Shift+M` | Toggle Device Mode |
| `Ctrl+L` (ใน Console) | Clear console |
| `Ctrl+Shift+R` | Hard refresh (ล้าง cache) |
| `Ctrl+P` (ใน DevTools) | เปิดไฟล์ (Sources) |
| `Esc` (ใน DevTools) | Toggle Console drawer |

---

## แบบฝึกหัด

### ฝึกหัดที่ 1: สำรวจเว็บ
1. เปิดเว็บไซต์ใดก็ได้ → เปิด DevTools
2. ใช้ Elements panel แก้ไขหัวข้อหลักของเว็บ
3. เปลี่ยนสีพื้นหลังของ body ผ่าน Styles panel

### ฝึกหัดที่ 2: Console Practice
1. เปิด Console แล้วพิมพ์: `document.querySelectorAll("a").length`
2. ดูว่าเว็บมี link กี่อัน
3. ลอง `console.table()` กับ array ที่สร้างเอง

### ฝึกหัดที่ 3: Network Investigation
1. เปิด Network panel → refresh หน้าเว็บ
2. นับจำนวน requests ทั้งหมด
3. หา request ที่ใช้เวลานานที่สุด
4. Filter เฉพาะ Fetch/XHR — มี API calls อะไรบ้าง?

---

## อ่านเพิ่มเติม
- [Chrome DevTools Documentation](https://developer.chrome.com/docs/devtools/)
- [MDN: What are browser developer tools?](https://developer.mozilla.org/en-US/docs/Learn/Common_questions/Tools_and_setup/What_are_browser_developer_tools)
