# EP06: Terminal & Command Line

## สิ่งที่จะได้เรียนรู้
- Terminal คืออะไร ทำไมต้องใช้
- คำสั่งนำทางและจัดการไฟล์
- Node.js & npm
- package.json
- การสร้างโปรเจกต์ใหม่

---

## 1. Terminal คืออะไร

โปรแกรมที่ให้คุณ **สั่งงานคอมพิวเตอร์ด้วยการพิมพ์คำสั่ง** แทนการคลิกเมาส์

### Terminal ในแต่ละ OS
| OS | Terminal |
|----|----------|
| Windows | PowerShell, Command Prompt, Windows Terminal |
| macOS | Terminal.app, iTerm2 |
| Linux | GNOME Terminal, Konsole |

> 💡 **VS Code มี Terminal ในตัว!** กด `Ctrl+`` เพื่อเปิด — สะดวกที่สุด

### ทำไมต้องใช้ Terminal
1. **เร็วกว่า** — คำสั่งเดียวแทนคลิกหลายขั้นตอน
2. **จำเป็นสำหรับ Dev** — รัน server, install packages, git, deploy
3. **AI ใช้ Terminal** — Agentic Coding tools รันคำสั่งผ่าน Terminal
4. **Automation** — เขียน script ทำงานซ้ำๆ อัตโนมัติ
5. **Server** — Server ส่วนใหญ่ไม่มี GUI ต้องใช้ Terminal เท่านั้น

---

## 2. คำสั่งนำทาง (Navigation)

```bash
# แสดงตำแหน่งปัจจุบัน (Print Working Directory)
pwd
# /Users/you/projects

# แสดงไฟล์และโฟลเดอร์
ls              # แสดงชื่อ
ls -l           # แสดงรายละเอียด (ขนาด, วันที่, สิทธิ์)
ls -la          # แสดงทั้งหมด รวมไฟล์ซ่อน (.gitignore, .env)
ls -lh          # แสดงขนาดแบบอ่านง่าย (KB, MB)

# เปลี่ยน directory
cd my-project      # เข้าโฟลเดอร์
cd ..              # ย้อนกลับ 1 ระดับ
cd ../..           # ย้อนกลับ 2 ระดับ
cd ~               # กลับ Home directory
cd /               # ไป root
```

### เคล็ดลับ
- **Tab Completion** — พิมพ์ชื่อไม่กี่ตัวแล้วกด `Tab` เพื่อ autocomplete
- **ลูกศรขึ้น ↑** — เรียกคำสั่งก่อนหน้า
- **Ctrl+C** — หยุดคำสั่งที่กำลังรัน
- **Ctrl+L** — Clear หน้าจอ (หรือพิมพ์ `clear`)

---

## 3. คำสั่งจัดการไฟล์

```bash
# สร้างโฟลเดอร์
mkdir my-project
mkdir -p src/components    # สร้างหลายระดับ

# สร้างไฟล์เปล่า
touch index.html
touch style.css script.js  # สร้างหลายไฟล์

# คัดลอก
cp file.txt copy.txt
cp -r folder/ folder-copy/  # คัดลอกโฟลเดอร์ (-r = recursive)

# ย้าย / เปลี่ยนชื่อ
mv old.txt new.txt            # เปลี่ยนชื่อ
mv file.txt src/              # ย้ายไปโฟลเดอร์ src

# ลบ
rm file.txt                   # ลบไฟล์
rm -rf old-folder/            # ลบโฟลเดอร์ทั้งหมด

# แสดงเนื้อหาไฟล์
cat index.html                # แสดงทั้งหมด
head -20 file.txt             # แสดง 20 บรรทัดแรก
tail -20 file.txt             # แสดง 20 บรรทัดท้าย
```

> ⚠️ **ระวัง:** `rm -rf` ลบถาวร ไม่มีถังขยะ! ตรวจสอบให้ดีก่อนรัน

---

## 4. Node.js

### Node.js คืออะไร
- Runtime สำหรับรัน JavaScript **นอก Browser**
- ใช้สร้าง server, scripts, command-line tools
- จำเป็นสำหรับ web development สมัยใหม่

### ติดตั้ง
- ดาวน์โหลดจาก [nodejs.org](https://nodejs.org/) — เลือก LTS version
- หรือใช้ version manager เช่น nvm (Node Version Manager)

```bash
# ตรวจ version
node --version    # v20.x.x
npm --version     # 10.x.x

# รัน JavaScript file
node script.js

# เปิด REPL (interactive mode)
node
> 1 + 2
3
> .exit
```

---

## 5. npm — Package Manager

### npm คืออะไร
- **N**ode **P**ackage **M**anager
- ติดตั้ง library/tools จาก [npmjs.com](https://www.npmjs.com/)
- มี packages มากกว่า 2 ล้าน!

### คำสั่งที่ใช้บ่อย

```bash
# สร้าง package.json ใหม่
npm init -y

# ติดตั้ง dependencies ทั้งหมดจาก package.json
npm install            # หรือ npm i

# ติดตั้ง package ใหม่
npm install axios      # เพิ่มเป็น dependency
npm install -D eslint  # เพิ่มเป็น devDependency

# ลบ package
npm uninstall axios

# รัน scripts
npm run dev            # รัน script "dev"
npm run build          # รัน script "build"
npm start              # รัน script "start" (ไม่ต้องมี run)
npm test               # รัน script "test"

# ดู packages ที่ติดตั้ง
npm list               # ทั้งหมด
npm list --depth=0     # เฉพาะ top-level

# อัปเดต packages
npm outdated           # ดูว่า package ไหน outdated
npm update             # อัปเดตทั้งหมด
```

---

## 6. package.json — สูตรของโปรเจกต์

```json
{
  "name": "my-project",
  "version": "1.0.0",
  "description": "โปรเจกต์ทดสอบ",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint ."
  },
  "dependencies": {
    "next": "^14.0.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "eslint": "^8.0.0"
  }
}
```

### อธิบายแต่ละส่วน
| ส่วน | ความหมาย |
|------|----------|
| `name` | ชื่อโปรเจกต์ |
| `version` | เวอร์ชัน (Semantic Versioning) |
| `scripts` | คำสั่งที่รันด้วย `npm run <name>` |
| `dependencies` | packages ที่ต้องใช้ใน production |
| `devDependencies` | packages ที่ใช้เฉพาะตอน develop |

### Semantic Versioning
`^14.2.1` → `MAJOR.MINOR.PATCH`
- **MAJOR** (14) — เปลี่ยนแบบ breaking changes
- **MINOR** (2) — เพิ่มฟีเจอร์ใหม่
- **PATCH** (1) — แก้ bug
- `^` = อัปเดต MINOR + PATCH ได้อัตโนมัติ

---

## 7. node_modules & package-lock.json

### node_modules
- โฟลเดอร์ที่เก็บ packages ที่ติดตั้ง
- ขนาดใหญ่มาก — **อย่า commit ขึ้น Git!**
- ใส่ใน `.gitignore` เสมอ
- ลบแล้วรัน `npm install` ใหม่ได้ทุกเมื่อ

### package-lock.json
- บันทึก **version ที่แน่นอน** ของทุก package
- ทำให้ทุกคนในทีมได้ package version เดียวกัน
- **ต้อง commit** ขึ้น Git

---

## 8. สร้างโปรเจกต์ใหม่

### Next.js (React Framework)
```bash
npx create-next-app@latest my-app
cd my-app
npm run dev
```

### Vite (React, Vue, Svelte)
```bash
npm create vite@latest my-app
cd my-app
npm install
npm run dev
```

> 💡 `npx` = รัน package โดยไม่ต้องติดตั้งถาวร — ใช้สำหรับ CLI tools ที่ใช้ครั้งเดียว

---

## แบบฝึกหัด

### ฝึกหัดที่ 1: Terminal Navigation
1. เปิด Terminal (หรือ VS Code Terminal)
2. ใช้ `pwd` ดูตำแหน่งปัจจุบัน
3. ใช้ `ls` ดูไฟล์ทั้งหมด
4. สร้างโฟลเดอร์ `practice` แล้ว `cd` เข้าไป
5. สร้างไฟล์ `index.html`, `style.css`, `script.js`

### ฝึกหัดที่ 2: npm Project
1. สร้างโฟลเดอร์ใหม่ → `npm init -y`
2. ดู package.json ที่ถูกสร้าง
3. ติดตั้ง package: `npm install dayjs`
4. ดู node_modules ที่เกิดขึ้น
5. สร้างไฟล์ `index.js` แล้วรันด้วย `node index.js`

### ฝึกหัดที่ 3: Create React App
1. รัน `npm create vite@latest my-first-app -- --template react`
2. `cd my-first-app` → `npm install` → `npm run dev`
3. เปิด Browser ดูผลลัพธ์

---

## อ่านเพิ่มเติม
- [MDN: Command line crash course](https://developer.mozilla.org/en-US/docs/Learn/Tools_and_testing/Understanding_client-side_tools/Command_line)
- [Node.js Documentation](https://nodejs.org/en/docs/)
- [npm Documentation](https://docs.npmjs.com/)
