# EP04: JavaScript พื้นฐาน (JavaScript Basics)

## สิ่งที่จะได้เรียนรู้
- JavaScript คืออะไร ทำอะไรได้บ้าง
- Variables & Data Types
- Functions
- Conditionals
- Arrays & Objects
- DOM Manipulation & Events

---

## 1. JavaScript คืออะไร

- **ภาษาโปรแกรม** ที่ทำให้เว็บมีความสามารถเชิงโต้ตอบ (Interactive)
- รันได้ทั้งใน **Browser** และ **Server** (Node.js)
- เป็นภาษาที่ AI / Agentic Coding ใช้เขียน **บ่อยที่สุด**

### วิธีเพิ่ม JavaScript เข้าเว็บ
```html
<!-- วิธีที่ 1: Internal (ใน HTML) -->
<script>
  console.log("สวัสดี!");
</script>

<!-- วิธีที่ 2: External (แยกไฟล์ — แนะนำ) -->
<script src="script.js"></script>

<!-- ใส่ก่อนปิด </body> เพื่อให้ HTML โหลดเสร็จก่อน -->
```

---

## 2. Variables — ตัวแปร

```javascript
// const — ค่าคงที่ (เปลี่ยนค่าไม่ได้)
const PI = 3.14159;
const name = "สมชาย";

// let — เปลี่ยนค่าได้
let score = 0;
score = 10;  // ✅ ได้

// var — แบบเก่า (ไม่แนะนำ)
var old = "หลีกเลี่ยง";
```

> ✅ **กฎง่ายๆ:** ใช้ `const` เป็นค่าเริ่มต้น → ถ้าต้องเปลี่ยนค่า ค่อยใช้ `let` → ไม่ใช้ `var`

---

## 3. Data Types — ประเภทข้อมูล

| Type | ตัวอย่าง | อธิบาย |
|------|----------|--------|
| **String** | `"สวัสดี"`, `'Hello'`, `` `Hi ${name}` `` | ข้อความ |
| **Number** | `42`, `3.14`, `-10` | ตัวเลข (จำนวนเต็มและทศนิยม) |
| **Boolean** | `true`, `false` | จริง/เท็จ |
| **Array** | `[1, 2, 3]` | รายการ/ลำดับข้อมูล |
| **Object** | `{ name: "A", age: 25 }` | ข้อมูลแบบ key-value |
| **null** | `null` | ไม่มีค่า (ตั้งใจกำหนด) |
| **undefined** | `undefined` | ยังไม่ได้กำหนดค่า |

### Template Literals
```javascript
const name = "สมชาย";
const age = 25;

// แบบเก่า
const msg1 = "สวัสดี " + name + " อายุ " + age + " ปี";

// แบบใหม่ (Template Literal — ใช้ backtick `)
const msg2 = `สวัสดี ${name} อายุ ${age} ปี`;
const msg3 = `ปีหน้าอายุ ${age + 1} ปี`;
```

---

## 4. Functions — ฟังก์ชัน

### Function Declaration
```javascript
function greet(name) {
  return `สวัสดี ${name}!`;
}
greet("สมชาย"); // "สวัสดี สมชาย!"
```

### Arrow Function (แบบใหม่ — ใช้บ่อยมาก)
```javascript
const greet = (name) => {
  return `สวัสดี ${name}!`;
};

// แบบสั้น (1 บรรทัด — ไม่ต้อง return)
const greet = (name) => `สวัสดี ${name}!`;

// ไม่มี parameter
const sayHi = () => "สวัสดี!";

// หลาย parameters
const add = (a, b) => a + b;
```

### Default Parameters
```javascript
const greet = (name = "เพื่อน") => `สวัสดี ${name}!`;
greet();          // "สวัสดี เพื่อน!"
greet("สมชาย");   // "สวัสดี สมชาย!"
```

---

## 5. Conditionals — เงื่อนไข

### if / else if / else
```javascript
const age = 18;

if (age >= 18) {
  console.log("เข้าได้");
} else if (age >= 13) {
  console.log("ต้องมีผู้ปกครอง");
} else {
  console.log("เข้าไม่ได้");
}
```

### Comparison Operators
| Operator | ความหมาย | ตัวอย่าง |
|----------|----------|----------|
| `===` | เท่ากัน (strict) | `5 === 5` → true |
| `!==` | ไม่เท่ากัน | `5 !== "5"` → true |
| `>` `<` | มากกว่า / น้อยกว่า | `10 > 5` → true |
| `>=` `<=` | มากกว่าหรือเท่ากับ | `5 >= 5` → true |
| `&&` | AND (และ) | `true && false` → false |
| `\|\|` | OR (หรือ) | `true \|\| false` → true |
| `!` | NOT (ไม่) | `!true` → false |

> ⚠️ ใช้ `===` แทน `==` เสมอ (strict equality ไม่แปลง type)

### Ternary Operator
```javascript
const status = age >= 18 ? "ผู้ใหญ่" : "เด็ก";
```

---

## 6. Loops — การวนซ้ำ

```javascript
// for loop
for (let i = 0; i < 5; i++) {
  console.log(i); // 0, 1, 2, 3, 4
}

// for...of (วนใน array)
const fruits = ["แอปเปิ้ล", "กล้วย", "ส้ม"];
for (const fruit of fruits) {
  console.log(fruit);
}

// forEach (method ของ array)
fruits.forEach((fruit, index) => {
  console.log(`${index}: ${fruit}`);
});
```

---

## 7. Arrays — รายการข้อมูล

```javascript
const fruits = ["แอปเปิ้ล", "กล้วย", "ส้ม"];

// เข้าถึงข้อมูล (index เริ่มจาก 0)
fruits[0];      // "แอปเปิ้ล"
fruits.length;  // 3

// เพิ่ม/ลบ
fruits.push("มะม่วง");    // เพิ่มท้าย
fruits.pop();              // ลบตัวท้าย
fruits.unshift("องุ่น");   // เพิ่มหน้า
fruits.shift();            // ลบตัวหน้า
```

### Array Methods สำคัญ (ใช้บ่อยมาก!)
```javascript
const numbers = [1, 2, 3, 4, 5];

// map — แปลงทุกตัว → ได้ array ใหม่
const doubled = numbers.map((n) => n * 2);
// [2, 4, 6, 8, 10]

// filter — กรองเฉพาะที่ตรงเงื่อนไข
const even = numbers.filter((n) => n % 2 === 0);
// [2, 4]

// find — หาตัวแรกที่ตรงเงื่อนไข
const found = numbers.find((n) => n > 3);
// 4

// reduce — รวมค่าทั้งหมด
const sum = numbers.reduce((acc, n) => acc + n, 0);
// 15

// includes — มีค่านี้ไหม
numbers.includes(3); // true
```

---

## 8. Objects — ข้อมูลแบบ key-value

```javascript
const user = {
  name: "สมชาย",
  age: 25,
  email: "somchai@email.com",
  skills: ["HTML", "CSS", "JS"],
};

// เข้าถึงข้อมูล
user.name;           // "สมชาย"
user["email"];       // "somchai@email.com"

// เพิ่ม/แก้ไข
user.phone = "081-xxx-xxxx";
user.age = 26;
```

### Destructuring — แกะข้อมูล
```javascript
const { name, age, email } = user;
console.log(name); // "สมชาย"

// เปลี่ยนชื่อ
const { name: userName } = user;
console.log(userName); // "สมชาย"

// Array destructuring
const [first, second] = ["A", "B", "C"];
console.log(first);  // "A"
console.log(second); // "B"
```

### Spread Operator
```javascript
// คัดลอก + แก้ไข object
const updated = { ...user, age: 26 };

// คัดลอก + รวม array
const all = [...fruits, "มังคุด"];
```

---

## 9. DOM Manipulation — ควบคุม HTML ด้วย JS

### เลือก Element
```javascript
// เลือกตัวเดียว
const title = document.querySelector("h1");
const btn = document.querySelector(".btn");
const main = document.querySelector("#main");

// เลือกหลายตัว
const items = document.querySelectorAll("li");
const cards = document.querySelectorAll(".card");
```

### เปลี่ยนเนื้อหา
```javascript
title.textContent = "หัวข้อใหม่";             // ข้อความ
title.innerHTML = "<em>หัวข้อใหม่</em>";      // HTML
```

### เปลี่ยนสไตล์
```javascript
title.style.color = "blue";
title.style.fontSize = "2rem";
```

### จัดการ Class
```javascript
title.classList.add("active");
title.classList.remove("active");
title.classList.toggle("hidden");
title.classList.contains("active"); // true/false
```

### สร้าง Element ใหม่
```javascript
const newItem = document.createElement("li");
newItem.textContent = "รายการใหม่";
list.appendChild(newItem);
```

---

## 10. Events — ตอบสนองผู้ใช้

```javascript
const btn = document.querySelector(".btn");

// คลิก
btn.addEventListener("click", () => {
  alert("คลิกแล้ว!");
});

// พิมพ์ใน input
input.addEventListener("input", (e) => {
  console.log(e.target.value);
});

// ส่งฟอร์ม
form.addEventListener("submit", (e) => {
  e.preventDefault(); // ป้องกัน refresh
  const formData = new FormData(form);
  console.log(formData.get("name"));
});
```

### Events ที่ใช้บ่อย
| Event | เมื่อไหร่ |
|-------|----------|
| `click` | คลิก |
| `dblclick` | ดับเบิลคลิก |
| `input` | พิมพ์ใน input |
| `change` | ค่าใน input เปลี่ยน |
| `submit` | ส่งฟอร์ม |
| `keydown` | กดปุ่มคีย์บอร์ด |
| `scroll` | เลื่อนหน้า |
| `load` | หน้าเว็บโหลดเสร็จ |
| `mouseover` | เมาส์ชี้ |
| `mouseout` | เมาส์ออก |

---

## 11. console.log — เครื่องมือ Debug

```javascript
console.log("ข้อความ");              // แสดงข้อความ
console.log("ค่า:", variable);       // แสดงตัวแปร
console.log({ name, age });          // แสดง object
console.error("มีข้อผิดพลาด!");     // สีแดง
console.warn("คำเตือน");            // สีเหลือง
console.table(arrayOfObjects);       // แสดงเป็นตาราง
```

> 💡 เปิด DevTools (F12) > Tab "Console" เพื่อดูผลลัพธ์

---

## แบบฝึกหัด

### ฝึกหัดที่ 1: Counter
สร้างหน้าเว็บที่มี:
- ตัวเลขแสดงค่า (เริ่มจาก 0)
- ปุ่ม "+" เพิ่มค่า
- ปุ่ม "-" ลดค่า
- ปุ่ม "Reset" กลับเป็น 0

### ฝึกหัดที่ 2: Todo List
สร้าง Todo List ที่:
- พิมพ์ข้อความแล้วกด Enter เพื่อเพิ่ม
- คลิกเพื่อ mark as done (ขีดฆ่า)
- มีปุ่มลบ

### ฝึกหัดที่ 3: Array Practice
```javascript
const students = [
  { name: "สมชาย", score: 85 },
  { name: "สมหญิง", score: 92 },
  { name: "สมศักดิ์", score: 78 },
  { name: "สมศรี", score: 95 },
];

// 1. หาคนที่คะแนนมากกว่า 80 (filter)
// 2. สร้าง array ของชื่อ (map)
// 3. หาคะแนนรวม (reduce)
// 4. หาคนที่คะแนนสูงสุด
```

---

## อ่านเพิ่มเติม
- [MDN: JavaScript First Steps](https://developer.mozilla.org/en-US/docs/Learn/JavaScript/First_steps)
- [MDN: DOM Manipulation](https://developer.mozilla.org/en-US/docs/Learn/JavaScript/Client-side_web_APIs/Manipulating_documents)
- [JavaScript.info](https://javascript.info/)
