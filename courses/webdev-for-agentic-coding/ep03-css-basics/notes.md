# EP03: CSS พื้นฐาน (CSS Basics)

## สิ่งที่จะได้เรียนรู้
- CSS คืออะไร เพิ่มเข้าเว็บอย่างไร
- CSS Selectors
- Box Model
- Colors & Typography
- Flexbox Layout
- Responsive Design

---

## 1. CSS คืออะไร

**CSS** = **C**ascading **S**tyle **S**heets

- กำหนด **รูปลักษณ์** ของเว็บไซต์ — สี, ฟอนต์, ขนาด, ตำแหน่ง, ระยะห่าง
- ทำงานคู่กับ HTML — HTML = โครงสร้าง, CSS = การตกแต่ง
- "Cascading" = สไตล์ถูกใช้ตามลำดับความสำคัญ (specificity)

---

## 2. วิธีเพิ่ม CSS เข้าเว็บ

### วิธีที่ 1: Inline Style (❌ ไม่แนะนำ)
```html
<p style="color: red; font-size: 18px;">สวัสดี</p>
```
- ผูกติดกับ element เดียว แก้ไขยาก ใช้ซ้ำไม่ได้

### วิธีที่ 2: Internal Style (⚠️ พอได้)
```html
<head>
  <style>
    p { color: red; }
  </style>
</head>
```
- ใส่ใน `<head>` ของ HTML — ใช้ได้กับหน้าเดียว

### วิธีที่ 3: External Stylesheet (✅ แนะนำ)
```html
<head>
  <link rel="stylesheet" href="style.css">
</head>
```
```css
/* style.css */
p { color: red; }
```
- แยกไฟล์ ใช้ซ้ำได้ทุกหน้า จัดการง่าย

---

## 3. CSS Syntax

```css
selector {
  property: value;
  property: value;
}
```

ตัวอย่าง:
```css
h1 {
  color: #3b82f6;
  font-size: 2rem;
  margin-bottom: 16px;
}
```

---

## 4. CSS Selectors

| Selector | เลือกอะไร | ตัวอย่าง |
|----------|----------|----------|
| `element` | ทุกตัวที่เป็น tag นั้น | `p { ... }` |
| `.class` | element ที่มี class นั้น | `.btn { ... }` |
| `#id` | element ที่มี id นั้น (เฉพาะตัว) | `#header { ... }` |
| `A B` | B ที่อยู่ข้างใน A (ลูกหลาน) | `nav a { ... }` |
| `A > B` | B ที่เป็นลูกตรงของ A | `ul > li { ... }` |
| `A, B` | ทั้ง A และ B | `h1, h2 { ... }` |
| `A + B` | B ที่อยู่ถัดจาก A ทันที | `h1 + p { ... }` |
| `:hover` | ตอนเอาเมาส์ชี้ | `a:hover { ... }` |
| `:focus` | ตอน element ถูก focus | `input:focus { ... }` |
| `:first-child` | ลูกคนแรก | `li:first-child { ... }` |
| `::before` | เพิ่มเนื้อหาก่อน element | `h1::before { ... }` |

### Specificity (ลำดับความสำคัญ)
1. `!important` — สูงสุด (หลีกเลี่ยงการใช้)
2. Inline style — `style="..."`
3. `#id` — 100 คะแนน
4. `.class`, `:pseudo-class` — 10 คะแนน
5. `element` — 1 คะแนน

---

## 5. Colors

### รูปแบบสีที่ใช้ได้
```css
/* ชื่อสี */
color: red;
color: blue;
color: transparent;

/* Hex (ใช้บ่อยสุด) */
color: #3b82f6;
color: #333;        /* shorthand */
color: #3b82f680;   /* มี opacity */

/* RGB */
color: rgb(59, 130, 246);
color: rgba(59, 130, 246, 0.5);  /* มี opacity */

/* HSL */
color: hsl(217, 91%, 60%);
color: hsla(217, 91%, 60%, 0.5);
```

### Property ที่เกี่ยวกับสี
```css
color: #333;              /* สีข้อความ */
background-color: #f5f5f5; /* สีพื้นหลัง */
border-color: #ddd;        /* สีขอบ */
```

---

## 6. Typography (ฟอนต์)

```css
font-family: 'Inter', 'Noto Sans Thai', sans-serif;
font-size: 16px;       /* ขนาด */
font-weight: 400;      /* น้ำหนัก: 100-900, normal=400, bold=700 */
font-style: italic;    /* ตัวเอียง */
line-height: 1.6;      /* ความสูงบรรทัด */
text-align: center;    /* จัดข้อความ: left, center, right, justify */
text-decoration: none;  /* ขีดเส้น: underline, line-through, none */
text-transform: uppercase; /* แปลง: uppercase, lowercase, capitalize */
letter-spacing: 0.5px;  /* ระยะห่างตัวอักษร */
```

---

## 7. Box Model

ทุก element ใน CSS เป็น "กล่อง" ที่ประกอบด้วย 4 ชั้น:

```
┌──────────── Margin ────────────┐
│ ┌────────── Border ──────────┐ │
│ │ ┌──────── Padding ───────┐ │ │
│ │ │ ┌────── Content ─────┐ │ │ │
│ │ │ │                    │ │ │ │
│ │ │ └────────────────────┘ │ │ │
│ │ └────────────────────────┘ │ │
│ └────────────────────────────┘ │
└────────────────────────────────┘
```

### CSS Properties
```css
.box {
  width: 300px;              /* ขนาดเนื้อหา */
  height: 200px;
  padding: 20px;             /* ระยะห่างภายใน */
  border: 2px solid #333;   /* ขอบ */
  margin: 16px;              /* ระยะห่างภายนอก */
  box-sizing: border-box;   /* ⬅️ สำคัญ! นับ padding+border รวมใน width */
}
```

> 💡 **แนะนำ:** ใช้ `box-sizing: border-box` เสมอ — ทำให้ `width` นับรวม padding และ border

### Margin & Padding Shorthand
```css
/* ทุกด้านเท่ากัน */
padding: 20px;

/* บน-ล่าง | ซ้าย-ขวา */
padding: 20px 40px;

/* บน | ซ้าย-ขวา | ล่าง */
padding: 10px 20px 30px;

/* บน | ขวา | ล่าง | ซ้าย (ตามเข็มนาฬิกา) */
padding: 10px 20px 30px 40px;

/* จัดกึ่งกลาง (block element) */
margin: 0 auto;
```

---

## 8. Flexbox

Flexbox เป็นระบบจัดวาง layout ที่ใช้บ่อยที่สุด:

```css
.container {
  display: flex;
  flex-direction: row;        /* row | column */
  justify-content: center;    /* จัดแนวหลัก (แนวนอนถ้า row) */
  align-items: center;        /* จัดแนวรอง (แนวตั้งถ้า row) */
  gap: 16px;                  /* ระยะห่างระหว่าง items */
  flex-wrap: wrap;            /* ขึ้นบรรทัดใหม่เมื่อล้น */
}
```

### justify-content (แนวหลัก)
- `flex-start` — เริ่มจากซ้าย (default)
- `center` — กึ่งกลาง
- `flex-end` — ไปทางขวา
- `space-between` — เว้นระยะเท่ากัน (ไม่มีระยะที่ขอบ)
- `space-around` — เว้นระยะรอบ items
- `space-evenly` — เว้นระยะเท่ากันหมด

### align-items (แนวรอง)
- `stretch` — ยืดเต็ม (default)
- `flex-start` — ชิดบน
- `center` — กึ่งกลาง
- `flex-end` — ชิดล่าง
- `baseline` — จัดตาม baseline ของข้อความ

### ตัวอย่างจัดกึ่งกลาง
```css
/* จัดกึ่งกลางทั้งแนวนอนและแนวตั้ง */
.center-everything {
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 100vh;
}
```

---

## 9. CSS Units (หน่วยวัด)

| หน่วย | ประเภท | อธิบาย | ใช้กับ |
|-------|--------|--------|--------|
| `px` | Absolute | pixel ตายตัว | border, shadow, เล็กน้อย |
| `rem` | Relative | เทียบกับ root font-size | font-size, spacing |
| `em` | Relative | เทียบกับ parent font-size | ภายใน component |
| `%` | Relative | เทียบกับ parent | width, height |
| `vw` | Viewport | % ของความกว้างหน้าจอ | full-width elements |
| `vh` | Viewport | % ของความสูงหน้าจอ | full-height sections |

---

## 10. Responsive Design

### Media Queries
```css
/* Mobile first approach */
.container {
  width: 100%;
  padding: 0 16px;
}

/* Tablet (768px ขึ้นไป) */
@media (min-width: 768px) {
  .container {
    max-width: 720px;
    margin: 0 auto;
  }
}

/* Desktop (1024px ขึ้นไป) */
@media (min-width: 1024px) {
  .container {
    max-width: 960px;
  }
}
```

### Breakpoints ที่นิยม
| ชื่อ | ขนาด | อุปกรณ์ |
|------|------|---------|
| Mobile | < 640px | โทรศัพท์ |
| Tablet | 640px - 1024px | แท็บเล็ต |
| Desktop | > 1024px | คอมพิวเตอร์ |

---

## แบบฝึกหัด

### ฝึกหัดที่ 1: Card Component
สร้าง Card ที่มี:
- รูปภาพด้านบน
- หัวข้อ, คำอธิบาย, ปุ่ม
- ใช้ Box Model จัดระยะห่าง
- มี hover effect

### ฝึกหัดที่ 2: Navigation Bar
สร้าง Navbar ที่:
- ใช้ Flexbox จัดวาง
- โลโก้ซ้าย, เมนูขวา
- Responsive — เมนูซ่อนบนมือถือ

### ฝึกหัดที่ 3: Responsive Grid
สร้าง Grid ของ Cards:
- Desktop: 3 คอลัมน์
- Tablet: 2 คอลัมน์
- Mobile: 1 คอลัมน์

---

## อ่านเพิ่มเติม
- [MDN: CSS first steps](https://developer.mozilla.org/en-US/docs/Learn/CSS/First_steps)
- [MDN: CSS Selectors](https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_selectors)
- [CSS-Tricks: A Complete Guide to Flexbox](https://css-tricks.com/snippets/css/a-guide-to-flexbox/)
- [MDN: Responsive design](https://developer.mozilla.org/en-US/docs/Learn/CSS/CSS_layout/Responsive_Design)
