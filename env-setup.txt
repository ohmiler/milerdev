# Copy these values into .env.local for local development only.
# Keep production and Railway credentials in the deployment platform, not here.

# ======================
# Database
# ======================
DATABASE_URL="mysql://root:root@localhost:3306/milerdev"

# ======================
# NextAuth
# ======================
AUTH_SECRET="your-secret-key-here"
AUTH_GOOGLE_ID="your-google-client-id"
AUTH_GOOGLE_SECRET="your-google-client-secret"

# ======================
# App URL
# ======================
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# ======================
# Email
# ======================
SMTP_HOST="smtp.example.com"
SMTP_PORT="587"
SMTP_USER="smtp-user"
SMTP_PASS="smtp-password"
SMTP_FROM="MilerDev <noreply@yourdomain.com>"

# ======================
# Payment - Stripe
# ======================
STRIPE_SECRET_KEY="sk_test_xxxxxxxxxxxx"
STRIPE_WEBHOOK_SECRET="whsec_xxxxxxxxxxxx"

# ======================
# Payment - SlipOK (PromptPay)
# ======================
SLIPOK_API_KEY="your-slipok-api-key"
SLIPOK_BRANCH_ID="your-branch-id"

# ======================
# Video - Bunny.net
# ======================
BUNNY_LIBRARY_ID="your-library-id"
BUNNY_API_KEY="your-bunny-api-key"
BUNNY_CDN_HOSTNAME="your-cdn-hostname.b-cdn.net"

# ======================
# Notes
# ======================
# 1. DATABASE_URL in .env.local should always point to local MySQL.
# 2. Production DATABASE_URL should be configured on Railway or Infisical.
# 3. Generate AUTH_SECRET with: openssl rand -base64 32
# 4. Use Stripe test keys during development.

# ======================
# New machine setup
# ======================
# 1. git clone -> npm install
# 2. Create .env.local from this file
# 3. Start MySQL: npm run docker:up
# 4. Push schema to local DB: npm run db:push:local
# 5. Seed test data: npm run db:seed
#
# Test logins:
#   Admin:      admin@milerdev.com / admin1234
#   Instructor: instructor@milerdev.com / instructor1234
#   Student:    student@milerdev.com / student1234
